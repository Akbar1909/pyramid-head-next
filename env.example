# Base URL of pyramid-api-nestjs (no trailing slash). Default: http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000

# Locales are configured in src/i18n/routing.ts (en, az). URLs use a prefix, e.g. /en/dashboard, /az/login.
